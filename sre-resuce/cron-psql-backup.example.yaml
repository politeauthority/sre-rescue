# Default values for sre-resuce.

replicaCount: 1
global:
  # Slack Token for sending messages
  slackCliToken:

image:
  repository: politeauthority/sre-rescue
  tag: latest
  pullPolicy: Always

tools:
  getSecrets: true
  # BackBlaze Credentials
  b2BucketName: aB2BucketName
  b2ApplicationKey: 012431234your123123123key
  b2ApplicationKeyID: ASJJH7-your-JHGAS76-Application-1235-ID
  b2BackupPath: path/frombucket/forbackup

postgresTools:
  enabled: true
  # Postgres Connection Info/ Credentials
  pgHost: pep-postgres-postgresql
  pgPort: 5432
  pgDbName: peppercon_evennia
  pgUser: blackpepper
  pgPass: tJdi5H3NpvRfvZb
  backup:
    enabled: true
    schedule: "*/5 * * * *"


nameOverride: ""
fullnameOverride: ""

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
